<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <link rel="icon" type="image/x-icon" href="resources/images/favicon.png">
    <title>{{ "AdonisJS" + " | " + title || "AdonisJS - A fully featured web framework for Node.js" }}</title>
    @if ($slots.meta)
    {{{ await $slots.meta() }}}
    @endif
    @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>

<body class="container mx-auto grid grid-rows-[auto_1fr] min-h-screen">
    <header class="p-4 flex items-center justify-between">
        <a href="/" class="font-bold text-2xl">⚡️ Adonis</a>
        <nav class="flex gap-4 items-center">
            <a href="{{route("home")}}">Home</a>
            <a href="{{route("movies.index")}}">Movies</a>
            <a href="{{route("dashboard")}}">Dashboard</a>
        </nav>
    </header>
    <main>
        {{{ await $slots.main() }}}
    </main>
</body>

</html>
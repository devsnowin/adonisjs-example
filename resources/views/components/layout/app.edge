<!DOCTYPE html>
<html class={{"dark"}}>
<head>
    <meta charset="utf-8">
    <link rel="icon" type="image/x-icon" href="resources/images/favicon.png">
    <title>
        {{ 
            "AdonisJS" + " | " + title || 
            "AdonisJS - A fully featured web framework for Node.js" 
        }}
    </title>
    
    @if ($slots.meta)
        {{{ await $slots.meta() }}}
    @endif
    
    @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>

<body class="container max-w-7xl mx-auto grid grid-rows-[auto_1fr] min-h-screen">
    @include('partials/header')
    <main class="p-4">
        {{{ await $slots.main() }}}
    </main>
    @include('partials/footer')
</body>
</html>
@layout.app({ title: "Writers" })

    {{-- {{inspect(writers[0])}} --}}

    <div class="space-y-6">
        <h1>Movie writers</h1>
        <ul class="grid grid-cols-3 gap-4">
            @each(writer in writers)
                @!cineast.card({cineast: writer, href: route('writers.show', { id: writer.id })})
            @end
        </ul>
    </div>

@end